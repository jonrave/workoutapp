{
  "name": "@peakspan/adapters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./claude": "./src/claude.ts",
    "./claude-chat": "./src/claude-chat.ts"
  },
  "description": "Ingestion adapters (contract Step 4): normalize form payloads, guided protocols, device payloads, and free text into EngineEvents. Adapters do nothing else.",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "@peakspan/engine": "*",
    "zod": "^4.1.0"
  },
  "scripts": {
    "test": "vitest run"
  }
}
